import React from "react";
import instagramAccountId from "../StepByStep.js";

function Comments(props) {
    const { facebookUserAccessToken } = props;
    const [showComments, setShowComments] = useState(false);
}

